name: Build Windows EXE
on: [push, workflow_dispatch]

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build Windows executable
      run: |
        npx electron-packager . "ELBER OS Loader" --platform=win32 --arch=x64 --out=release --overwrite --asar
      
    - name: Upload Windows EXE
      uses: actions/upload-artifact@v4
      with:
        name: ELBER-OS-Windows
        path: release/ELBER\ OS\ Loader-win32-x64/
        retention-days: 7
